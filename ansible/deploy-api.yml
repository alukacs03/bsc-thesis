- hosts: api
  gather_facts: false
  vars:
    local_bin: "{{ playbook_dir }}/build/gluon-api"
    tmp_bin: /tmp/gluon-api
    dest_bin: /usr/local/bin/gluon-api
  tasks:
    - ansible.builtin.copy: { src: "{{ local_bin }}", dest: "{{ tmp_bin }}", mode: "0755" }
    - ansible.builtin.command: install -o root -g root -m 0755 "{{ tmp_bin }}" "{{ dest_bin }}"
      become: true
    - ansible.builtin.systemd:
        name: gluon-api
        state: restarted
      become: true
